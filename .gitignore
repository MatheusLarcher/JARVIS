__pycache__/
*.pyc
server/data/jarvis.db
server/data/tts_cache/
.env
node_modules/
apps/web/dist/
apps/android/**/build/
apps/wear/**/build/
apps/android/local.properties
apps/wear/local.properties
*.keystore
.gradle/
releases/
apps/desktop/node_modules/
apps/desktop/dist/
server/data/jarvis.log
server/data/voice.log
server/data/watchdog.log
apps/desktop/build/web/
# gerado pelo sync-web: guarda a raiz do projeto NESTA maquina
apps/desktop/build/projeto.json
server/data/*.log.old
tests/.cache/
# ---------------------------------------------------------------------------
# AUDIO — o que pode e o que nao pode ir pro repositorio publico
#
#  VAI:      server/data/voice/jarvis_ref.wav  (a voz do JARVIS, referencia do
#            clone — quem clonar o repo regera a biblioteca com ela)
#  NAO VAI:  a voz do Matheus (gravacoes dos pedidos) e o JARVIS respondendo
#            (biblioteca e cache do TTS) — audio de uso, fica so na maquina
# ---------------------------------------------------------------------------
server/data/gravacoes/
server/data/library/

# credenciais dos aparelhos (use o devices.example.yml como modelo)
config/devices.yml
config/devices.yml.bak-*
